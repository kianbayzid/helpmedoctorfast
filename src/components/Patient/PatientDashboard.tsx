import React from 'react';

// PatientDashboard - Removed (moved to .bak file)
// This is a placeholder to prevent import errors
const PatientDashboard: React.FC = () => {
  return (
    <div>
      <h1>Patient Dashboard has been removed</h1>
      <p>Only doctor dashboard is available now.</p>
    </div>
  );
};

export default PatientDashboard;